# Linting is now handled by the 'lint' job in tests.yml
# This file is kept for backwards compatibility but does nothing.
name: linter
on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
    - run: echo "Linting moved to tests.yml"
